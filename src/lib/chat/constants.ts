// Shared constants used by both the chat API route (server) and the chat panel (client).
// Keep in sync: the FALLBACK_MARKER must be a prefix of the phrase Claude is instructed to output.
export const FALLBACK_MARKER =
  'Dazu finde ich gerade keinen passenden Artikel im Hilfe-Center'
